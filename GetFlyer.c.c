#include <stdio.h>#include <stdlib.h>#include <string.h>#include <fcntl.h>#include <errno.h>#include <sys/types.h>#include <unistd.h>#define SIZE 256void errorMsg(char* str);/*This command displays flyers.*/int main(int argc, char* argv[]){	int file, rbytes, wbytes, len = 0;	char name[30] = { 0 }, buffer[SIZE] = { 0 };	if (argc != 2)		errorMsg("Incorrect amount of args/parms");	//Creating the file name.	strcat(name, "Black_Friday/");	strcat(name, argv[1]);	strcat(name, ".txt");	//Opening the targeted file.	if((file = open(name, O_RDONLY)) == -1)	{			len = strlen("Company Not Found!\n");		write(0, "Company Not Found!\n", len);		exit(1);	}	//Reading the entire file into buffer.	if ((rbytes = read(file, &buffer, SIZE)) == -1)		errorMsg("read");	if ((wbytes = write(0, buffer, strlen(buffer))) == -1)		errorMsg("write");	write(0, "\n", 1);	close(file);	return 0;}/*This function will be called in case there's an error and the program needs to be terminated.*/void errorMsg(char* str){	perror(str);	exit(1);}