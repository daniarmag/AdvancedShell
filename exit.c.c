#include <stdio.h>#include <stdlib.h>#include <string.h>#include <fcntl.h>#include <errno.h>#include <sys/types.h>#include <unistd.h>void errorMsg(char* str);/*This command removed Black_Friday folder and writes Goodbye.*/int main(int argc, char* argv[]){	int pid;	if (argc != 1)		errorMsg("Incorrect amount of args/parms");	//Removing the folder, -r for directory, -f for protected files.	if((pid = fork()) == 0)	{		execl("/bin/rm", "rm", "-r", "-f", "Black_Friday", NULL);		errorMsg("Error");	}	else if (pid == -1)		errorMsg("Fork failed.");	wait();	write(0, "Goodbye...\n", 11);	return 0;}/*This function will be called in case there's an error and the program needs to be terminated.*/void errorMsg(char* str){	perror(str);	exit(1);}