#include <stdio.h>#include <stdlib.h>#include <string.h>#include <fcntl.h>#include <errno.h>#include <sys/types.h>#include <unistd.h>#define SIZE 256void errorMsg(char* str);/*This command writes the amount of stores that participate in the campaign.*/int main(int argc, char* argv[]){	int file, rbytes, cnt = 0, i;	char buffer[SIZE] = { 0 };	if (argc != 1)		errorMsg("Incorrect amount of args/parms");	if((file = open("Black_Friday/camp_partic.txt", O_RDONLY)) == -1)		errorMsg("open");	//Reading the entire file into buffer.	if ((rbytes = read(file, &buffer, SIZE)) == -1)		errorMsg("read");	//Counting the of lines in the file = amount of stores.	for (i = 0; i < strlen(buffer); i++)	{		if (buffer[i] == '\n')			cnt++;	}	printf("%d companies take part in the Campaign\n", cnt);	close(file);	return 0;}/*This function will be called in case there's an error and the program needs to be terminated.*/void errorMsg(char* str){	perror(str);	exit(1);}